/* de verão, de inverno, para banho e íntimas

desconto: 5% (verão), 10% (inverno), 4% (banho) e 7% (íntimas). */

const descontoTotal = (
	nome: string,
	preco: number,
	classificacao: string
) => {
    
};
